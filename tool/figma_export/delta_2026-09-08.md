# What the moved design changed — measured against the built app

Renders in `renders/`. Node ids are from page `124:2`.

## 1. Onboarding — matches, one nit

Splash -> "Inhale Deeply" -> "Exhale Slowly" -> a 3-slide carousel. Built
correctly. The only difference: the design sets the slide body in Title Case
("Connect With Therapists Who Speak Your Language For Regular Sessions To
Manage Stress And Improve Mental Health.") and the app ships sentence case.

## 2. Mood checker — REDESIGNED (built 2026-09-09)

Not a tweak. The screen is a different interaction entirely.

**Built today:** a 3x3 grid of emoji tiles — Calm, Happy, Angry, Weak,
Fearful, Sad, Anxious, Stress, Depress.

**Designed now:** one large character illustration, a slider beneath it
labelled "Awful" on the left and "Awesome" on the right, and an "Add Detail"
primary button. The heading "How do you feel today?" stays.

The illustration is bound to the slider — it is the same character redrawn per
mood, and dragging changes the expression:

| slider | expression |
|---|---|
| far left (0) | male: scowling / female: sobbing |
| ~25% | shocked, mouth open |

There are separate **male and female** illustration sets, so the character
follows the user rather than being fixed. Eight frames exist across the two
sets; only three were rendered, so the full mood-to-artwork mapping still
needs reading before building.

Artwork is not exported yet — these are illustrations, not emoji, and each
needs a PNG per mood per gender.

**Mood record** (`176:31537`) is close to what is built: "Nice job today,
Dera", a Sun-Sat strip with filled ticks for completed days, "Come back
tomorrow, or explore your other recommendations.", and an "Explore" button.

## 3. Renames — done

See `rename_2026-09.md`. Note `profile_dashboard.png` still reads "Meditation
minutes" / "Balance minutes": the rename is incomplete *in Figma*, and the app
is deliberately ahead of it per an explicit instruction.

## 4. Profile — three real gaps (all closed 2026-09-09)

**Unsigned** (`176:34542`). The design wraps the whole prompt in a blue
gradient card the app does not draw at all:

- a settings gear at top-left of the screen (app has none here)
- inside the card: three white line icons in a row — a clock, a calendar with
  a magnifier, and a head with a heart
- the copy, confirmed as "Create an account to save your progress and see your
  stats" (this settles the open question about the ending)
- a white "Sign up" button *inside* the card, with a person glyph. The app
  says "Sign up or Log in" on a plain blue button outside any card.
- "Unclock Soothify Pro" outlined below — the design's typo; the app correctly
  ships "Unlock".

**Dashboard** (`176:34109`). Structure matches, but the design puts a line
icon above *every* stat — a seated meditation figure, a standing yoga figure,
and a video camera. The app draws only the camera, over the third.

**History** (`176:34144`). Two differences:

- the calendar sits inside a white rounded card with a hairline border; the
  app draws it bare on the background
- a day is selected, shown as a solid orange circle (the 2nd). The app has no
  selected-day state here, and adds a disabled "Select date" button the design
  does not have.

One caution: the design's calendar is a mock — it puts 1 August 2024 under
Mon, and 1 August 2024 was a Thursday. The app's calendar is correct. Do not
"fix" the app to match the render.

## 5. New since the old file

The moved file is a superset of what the app was built from. Not built, in
rough order of value:

- **AI Hub** (`176:56425`, `176:56395`) — real screens for the assist button,
  which is still a stub. Unexpanded: a droplet-over-water animation panel with
  a pause control and the label "Tap Droplet or Chat to Expand"; a stat row of
  Vibe / Breathing Pace / Ease Level ("Morning Mist", "4.7mm", "94.2%"); a
  "Select Environment Vibe" segmented control of Sunset Calm / Morning Mist /
  Evening Breeze; and a "Breathe Together" button. Expanded: a "Wellness
  Guide" chat over the same scene, with an online dot, a close X, assistant
  bubbles left in white and user bubbles right in blue, and an "Ask
  anything..." composer.
- **Community manager role** — ~20 frames: dashboard, subscribers, new and
  banned subscribers, report and moderation flows, notifications.
- **Corporate billing** — corporate form, success, payment states.
- **Discovery search** — 10 frames plus searched-result, no-result and input
  states.
- **Auth extras** — Face ID, Touch ID, Login/error, Create username.
- **KYC condition variants** — `Kyc screen | Stress`, `Kyc screen | Anxiety`.
- **Article** (`176:30889`).

---

# Built 2026-09-09

## Mood checker

Rebuilt as the design draws it: one illustration over a slider from "Awful" to
"Awesome", with an "Add Detail" button. The 3x3 emoji grid and `MoodTile` are
gone.

Layout is pinned to points measured off the frame, below the app bar: heading
44, figure 154 (203 tall), slider track centre 464, labels 489, button 570.
Slider colours sampled from the render — active `#638FE8`, inactive `#DADADA`,
handle and end labels `#2F6FED`.

**Four steps, two characters.** The artwork changes in bands at 20%, 45% and
70% of the track — read off where the design parked the handle for each
illustration, not even quarters. The male set has only three distinct faces
across its four frames, so its top two steps share one; the female set has
four. Which character shows comes from the KYC "What is your gender?" answer;
anything that is not an explicit "male" gets the female set, because it is the
only one drawn for all four steps.

**The data model changed.** `Mood` (nine named moods) became a `score` of 0..1
on `MoodEntry`, with `MoodLevel` derived from it. `MoodEntry.fromJson` still
reads the old `mood` key and maps it onto the scale, so an upgrading user
keeps their calendar and streak instead of losing them.

**Artwork extraction.** `/nodes` was rate-limited, so the illustrations could
not be exported as their own nodes. They were recovered from the rendered
frames instead: the same pose exists over a light `#F5FBFF` ground and a dark
`#1A1A1B` one at identical pixel positions, which makes alpha exactly
solvable — `(1-a) = (C_light - C_dark) / (B_light - B_dark)`, then
`F = (C_light - (1-a)B_light)/a`. Keying against one background alone left a
light halo on the female afro. Files are `assets/images/mood/{male,female}_*`.

**Open:** "Add Detail" has no frame behind it — no note composer is drawn for
this flow anywhere in the file. It commits the score and continues to the
records screen, which is where the old flow ended. If it is meant to open the
journal composer, say so.

## Profile

**Unsigned.** The blue card is built: `brandInk` to `brandLight` top to
bottom, three white line icons, the pitch, and a white "Sign up" button with a
person glyph inside the card. A settings gear sits top-left — which also
settles a question left open in the code, since no signed-in frame shows how
Settings is reached. Copy confirmed as "…and see your stats".

**Dashboard.** The stat glyphs are exported as black line art and were
invisible on the deep blue card, which is why only the camera ever showed.
They are tinted to the card's foreground now. The profile goldens also never
precached their PNGs, so they recorded blank boxes and hid the bug — they do
now.

**History.** The calendar moved inside a white card with a `#D8D8D8` hairline,
the chosen day fills `#FF9500` rather than the app's usual blue, and the
"Select date" button is gone — the frame has none, and a permanently disabled
button under an empty calendar read as a dead end. `MonthCalendar` gained an
optional `selectedColor` so the other three calendars that share it keep the
blue.

The frame's own calendar prints 1 August 2024 under Monday; it was a Thursday.
The app's grid is correct — do not match the render there.

**Icons.** The gear, the person glyph and the three card icons were cropped
from the render for the same reason as the mood art, and keyed to transparent.
They live in `assets/images/profile/`.

## AI Hub (built 2026-09-09)

`176:56425` (Unexpanded) and `176:56395` (Expanded | Chat), reached from the
floating assist button — which until now raised "not built yet".

**One screen, two states**, not two routes. The frames are the same scene and
the collapsed one says so: "Tap Droplet or Chat to Expand".

Collapsed: the scene panel over three readouts (Vibe / Breathing Pace / Ease
Level), a rule, "Select Environment Vibe" as a three-way segmented control
(Sunset Calm / Morning Mist / Evening Breeze) and "Breathe Together".
Expanded: the same scene full-bleed behind the Wellness Guide conversation,
with a live dot, a close button and an "Ask anything..." composer.

Measured below the status bar: panel 52 (233 tall), readouts 314, rule 371,
section label 400, segments 421 (40 tall), button 485 (52 tall). Colours
sampled from the render — stat caption `#72737A`, value `#1B1F26`, rule
`#EBF1F5`, segment track `#FEFEFE` inside `#EAEAEA`, chosen segment
`actionFill`.

### The scene is painted, not exported

The frames bake "Tap Droplet or Chat to Expand" into the artwork, so a crop
would carry a label that cannot be changed, localised, or removed once the
panel expands. It is a `CustomPainter` instead: a sky gradient sampled down
the frame's left edge, three wave bands with hairline crests, a horizon glow,
and the droplet as a halo, a radial-filled egg, a hairline outline and a
highlight. That also gives the droplet its bob, which is what the guide's own
copy asks the user to breathe along with.

Two mistakes worth remembering:

- Sizing features as fractions of the *box* stretched the egg into a tall thin
  oval once the chat took the whole screen. Everything is in design units
  (341.5x233) now.
- Scaling to *cover* then ballooned the droplet to a quarter of the screen.
  The frames keep it the same size in both states, so the scene scales by
  width and anchors its waterline to the same fraction of the box, letting the
  sky grow instead.

### Open

- The readouts "4.7mm" and "94.2%" are session telemetry with no breathing
  engine behind them, so they are fixed strings. Only Vibe is live.
- Sending a message appends the user's line and says the guide is not
  connected. No model is wired, and a canned reply would look like an answer.
- The frame repeats one guide line as filler for its lower bubbles; that is
  mock copy, so only the two real opening lines are seeded.

## Notifications (built 2026-09-09)

`176:24737`, reached from the bell on Home — which had a `GestureDetector`
with no `onTap` at all, a dead control on every build until now.

The file has two notification frames. `176:36688` puts every entry in an
identical outlined card; `176:24737` is the mixed one and is the one built.
Its rows are deliberately different shapes, so each is a `NotificationKind`
and the list renders whichever it is handed:

| kind | shape |
|---|---|
| `social` | avatar, bold actor, rest of the line, timestamp |
| `reminder` | outlined card: red dot, icon, "Reminder", body line |
| `digest` | red dot, filled heart, heading, centred body — no border |
| `plain` | line with no avatar, timestamp |
| `action` | line, an inline "Read" link, timestamp |

Measured below the status bar: header 36, chips 99 (20 tall, radius 10),
first row 148, content margin 24. Chips — chosen one filled `#2F6FED`, the
others outlined `#ECEDED`. Reminder card border `#E6EBEE`. Unread dot
`#F45A4F`, 7.5. The title and the "Read" link are painted with the app's
existing `titleGradient`, which is what the sampled `#1E396E` turned out to be.

### Decisions

- **Timestamps are computed from a `DateTime`**, not stored strings. The frame
  writes "45minutes ago", "1 days ago" and "7minutes" — typos, and a stored
  string would be wrong the second time the screen opened. `AppNotification.ago`
  formats them properly, and the repository takes an injectable `now` so the
  goldens do not go stale by the hour.
- One consequence: "45 minutes ago" is ~11 wider than the frame's
  "45minutes ago", so the first row ellipsises at "commented on yo...". The
  source text is truncated post content ending in "..." anyway; correct
  spelling was worth the character.
- The frame writes "1 day ago" for Paul and "Yesterday" for John, both a day
  old. One formatter gives both "Yesterday".
- The reminder still says **Yoga**. The rename covered the three section
  names, not the practice, and this frame post-dates it.
- "Read" has nothing behind it — the Article frame (`176:30889`) is in the
  file but not built — so it says so.

### Assets

`/nodes` is still rate-limited, so the two avatars, the reminder glyph and the
solid heart were cropped from the render and keyed to transparent, as the mood
and profile art were. The frame gives Kendrick and Paul the same portrait, so
there are two avatars, not three. Both glyphs are navy line art from a
light-mode frame and are tinted at runtime, or they vanish on the dark palette.

## There are TWO mood pickers, not one (corrected 2026-09-09)

I got this wrong first time. Every `Mood checker/*` frame sits under the
"Home Screen - Mood checker - Ai Assistant" banner, so I treated them as one
screen's theme and gender variants and built only the Home slider. The
onboarding one is a different screen in a different section, and I missed it.

| | onboarding | after onboarding |
|---|---|---|
| frame | `Kyc screen | Stress` `176:23723`, `| Anxiety` `176:56150` | `Mood checker/*` |
| where | KYC step 1, "What brings you to Soothify?" | Home, behind the Mood Checker card |
| input | swipeable carousel of illustrations, multi-select | a single slider, Awful to Awesome |
| ground | full-bleed in the focused option's colour | the app background |

The carousel is what the earlier note "illustration that user can swipe into
focus" was describing — it is the onboarding one, not the Home one.

### Built

KYC step 1 is now `KycInput.carousel`: full-bleed colour, the illustrations on
a rail with both neighbours peeking, the label beneath, and a white Next. The
emoji tiles it used to draw are gone — the design has not used emoji since the
move.

Measured: progress 84 (4 tall), prompt 145.5, subtitle 179.5, illustration
band 297.5 (224 tall), label 562.5, button 697. Stress floods `#F9980F` with a
`#4679ED` progress accent; Anxiety floods `#7B7FE8` with `#FFAE24`. The accent
contrasts the ground rather than matching it, which is why it is stored per
option.

### Open

- **Two of the four illustrations are missing.** The remaining frames
  (`176:56161`, `176:56173`, both misnamed "Anxiety" in the file) were not
  rendered before the image endpoint rate-limited, so "Sleep disorder" and
  "Depression" carry no artwork and fall back to the brand ground. One render
  call and two extractions finishes it.
- **The frames draw no selected state** — they only ever show one option in
  focus — but the step is multi-select. A white tick badge on the focused
  figure is mine, not the design's.
- **The progress bar shows six segments, the frames four.** The questionnaire
  has six questions; the frames' bar has always disagreed with that and the
  bar is driven by the question count. Unchanged here, but worth settling.

## Profile check-in screens — read at last (2026-09-09)

`176:34213` (Check-Ins tab, 390x1352), `176:34264` (the mood calendar),
`176:34492` (one entry). `CheckinScreen` and `ProfileCheckins` were built from
the OLD file and had never been compared against these.

**These frames pre-date the mood redesign.** They still draw the nine-mood
emoji vocabulary — the calendar puts an emoji in a day's circle, and the entry
card reads "🙁 Sad" — and they still say "Daily Meditation" and "Daily
Balance", the names that were renamed. The Profile section of the file has not
been brought forward, exactly as `Profile/dashboard` still saying "Meditation
minutes" showed.

So the layout was taken from them and the vocabulary was not.

### Built

- **The Check-Ins cards** now carry a badge above a centred title, 246 tall as
  the frame draws them. Daily Pilates & Core and Daily Stretch & Restore reuse
  the Explore tiles — the frame draws exactly those, so nothing was exported
  twice.
- **The mood calendar** is months in bordered cards, each day a 20 circle
  (`#EAE7E3`) with its number beneath, the recorded day's mark inside it.
- **The header carries its action** — a three-dot glyph on the calendar, a
  calendar-search glyph on an entry. The frames are two screens, and that
  second glyph is what returns; picking a day that has something recorded
  swaps the calendar for the entry.
- **The entry card** is the date line over the mark and its label, inset 40
  rather than the screen's 24.
- The frame has no confirm button on this screen. The earlier build put a
  "Select date" under every month; it is gone, as it went from History.

### The mood vocabulary

Settled to match the pickers: a recorded mood is the **slider's illustration
and its level**, not the old nine-mood emoji and their names. The calendar
puts the figure inside the day's circle and the entry card puts it beside the
level — cropped to the head, because at 20 across a bust reads as nothing and
the face is what carries the mood.

### Two frame errors not copied

- The frame's grid runs **ten days to a row** beneath a Mon..Sun header, which
  those labels cannot describe. The real seven-column week is kept; only the
  circle-and-number treatment is taken from the frame.
- As on Profile/History, the frame's own calendar does not agree with the
  actual weekdays of the month it prints.

## Gender before the concerns carousel (2026-09-11)

The questionnaire now opens on "What is your gender?" and the concerns
carousel is second. The order matters, not just the content: the carousel
draws a gendered figure, so the answer has to exist by the time that step is
reached. `KycOption.illustrationFor(gender)` picks the set; only an explicit
"male" switches, everything else — female, non-binary, undisclosed, no answer
— draws the female figure, the same rule the Mood Checker uses.

**Both sets are in.** The male carousel is a new row of four frames at y=0
(`191:36092`-`191:36125`), same grounds and accents as the female set; the
figures were lifted with `tool/extract_figure.py` as before. One thing to
know: the designer labelled his first frame "Stressed" where hers says
"Stress". The option label is gender-neutral and stays "Stress".

Test fallout was instructive: five tests assumed step 0 was `concerns` and
called `choose()` on it while the controller sat on `gender`, quietly writing
the concern under the gender key. They step through explicitly now, and the
golden test looks its steps up by id so a bare index cannot point at the
wrong screen again.

## Home: the Explore tiles are photographs now (2026-09-11)

`191:5489` (light, signed in). The three tiles are 100x90 photos filling the
tile edge to edge, radius 8, no card and no border — a pilates backbend, a
therapist portrait, a forward stretch. Same footprint as before; only the
treatment changed. The label under the middle one reads "Book a **L**icensed
Expert" now, and that casing has been applied everywhere the name appears.

The illustrated badges the tiles used to carry are still what the Check-Ins
cards and the library header draw, so those files stay under their old names
and the photos are new ones (`explore/pilates_core.jpg` etc.).

Two frames not to build from: `191:10395`, the dark Home, still carries the
old badges — the dark variant was not brought forward; and `191:10877` is a
duplicate that has had its content emptied (a chevron and three hearts on a
dark ground). Neither is a design decision.

The Explore-tile → library-header Hero still runs. Its two ends no longer
match — a photo flies into an illustration slot — which reads as a flight
rather than a morph, and is left in place until the library header is
re-read against the new file.

